@model WebApplication8.Models.Task
@{
    ViewData["Title"] = ViewBag.PageTitle ?? "Add Task";
    
    // Get the list of categories passed from the controller
    // This will populate the dropdown dynamically from the database
    var categories = ViewBag.Categories as List<WebApplication8.Models.Category>;
    var formAction = ViewBag.FormAction as string ?? "CreateTask";
}
    <h1 class="mb-4">@ViewData["Title"]</h1>

    <div class="row">
        <div class="col-lg-8">
            <div class="card shadow-sm">
                <div class="card-body">
                    <!-- Form posts to CreateTask or Edit based on the action supplied by the controller -->
                    
                    <form asp-action="@formAction" method="post">
                        <input type="hidden" asp-for="TaskItemId" />
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <!-- Task Name -->
                        <div class="mb-3">
                            <label asp-for="TaskName" class="form-label"></label>
                            <input asp-for="TaskName" class="form-control" />
                            <span asp-validation-for="TaskName" class="text-danger"></span>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="DueDate" class="form-label"></label>
                                <input asp-for="DueDate" class="form-control" type="date" />
                                <span asp-validation-for="DueDate" class="text-danger"></span>
                            </div>

                            <div class="col-md-6 mb-3">
                                <label asp-for="Quadrant" class="form-label"></label>
                                <select asp-for="Quadrant" class="form-select">
                                    <option value="">Select...</option>
                                    <option value="1">Quadrant I (Important / Urgent)</option>
                                    <option value="2">Quadrant II (Important / Not Urgent)</option>
                                    <option value="3">Quadrant III (Not Important / Urgent)</option>
                                    <option value="4">Quadrant IV (Not Important / Not Urgent)</option>
                                </select>
                                <span asp-validation-for="Quadrant" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="CategoryId" class="form-label">Category</label>
                            <select asp-for="CategoryId" class="form-select">
                                <option value="">Select...</option>
                                @if (categories != null)
                                {
                                    foreach (var c in categories)
                                    {
                                        <option value="@c.CategoryId">@c.CategoryName</option>
                                    }
                                }
                            </select>
                            <span asp-validation-for="CategoryId" class="text-danger"></span>
                        </div>

                        <div class="form-check mb-3">
                            <input asp-for="Completed" class="form-check-input" />
                            <label asp-for="Completed" class="form-check-label"></label>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">Save</button>
                            <a asp-controller="Home" asp-action="Quadrants" class="btn btn-outline-secondary">Cancel</a>
                        </div>
                    </form>

                </div>
            </div>
        </div>
    </div>
